class Solution(object):
    def twoSum(self, nums, target):
        sum = 0
        for i in range(len(nums)):
           for j in range(1,len(nums)):

             if i == j:
               break

             sum = nums[i] + nums[j]

             if target == sum:
               return [i,j]
